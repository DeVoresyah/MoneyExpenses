import { connect } from '../../Themes/OsmiProvider'

export default connect({
  container: "w-44 h-44 absolute bg-secondary rounded-full items-center justify-center bottom-10 right-10",
  iconAdd: "w-16 h-16"
})
